{
  "tested_candidates": {
    "Gold-Bond Defensive Pair Switch": {
      "strategy": "Gold-Bond Defensive Pair Switch",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport statistics\nimport math\n\ndef compute_log_returns(closes):\n    \"\"\"Daily log returns for vol calculation.\"\"\"\n    lr = [0.0]\n    for i in range(1, len(closes)):\n        lr.append(math.log(closes[i] / closes[i-1]))\n    return lr\n\ndef realized_vol(log_rets, i, window):\n    \"\"\"Annualized realized volatility over trailing `window` days ending at i.\"\"\"\n    if i < window:\n        return None\n    seg = log_rets[i-window+1:i+1]\n    sd = statistics.pstdev(seg)\n    return sd * math.sqrt(252)\n\ndef trailing_perf(closes, i, window):\n    \"\"\"Trailing return over `window` days ending at i.\"\"\"\n    if i < window:\n        return None\n    return closes[i] / closes[i-window] - 1\n\ndef vol_rotation_strategy(spy, gld, tlt):\n    \"\"\"\n    Regime-switching vol strategy.\n\n    - Compute 20-day realized vol of SPY.\n    - When SPY vol > its own trailing 100-day median vol => HIGH VOL regime:\n      rotate into the better trailing-20-day performer of GLD vs TLT.\n      WHY: in stressed/high-vol regimes equities tend to sell off; we seek\n      the stronger defensive/safe-haven asset (gold vs long bonds) as a hedge.\n    - Otherwise (calm regime): hold SPY.\n      WHY: low volatility typically favors continued equity trend/carry.\n    - Rebalance weekly (every 5 trading days); positions held constant between.\n    \"\"\"\n    n = len(spy)\n    spy_closes = [b[\"c\"] for b in spy]\n    gld_closes = [b[\"c\"] for b in gld]\n    tlt_closes = [b[\"c\"] for b in tlt]\n\n    spy_lr = compute_log_returns(spy_closes)\n\n    VOL_WINDOW = 20\n    MEDIAN_WINDOW = 100\n    PERF_WINDOW = 20\n    REBAL_EVERY = 5\n\n    rets = []\n    # current holding: \"SPY\", \"GLD\", or \"TLT\"\n    holding = \"SPY\"\n\n    for i in range(n):\n        # daily return based on yesterday's holding decision\n        r = 0.0\n        if i > 0:\n            if holding == \"SPY\":\n                r = spy_closes[i] / spy_closes[i-1] - 1\n            elif holding == \"GLD\":\n                r = gld_closes[i] / gld_closes[i-1] - 1\n            elif holding == \"TLT\":\n                r = tlt_closes[i] / tlt_closes[i-1] - 1\n        rets.append(r)\n\n        # weekly rebalance decision (applied for next days)\n        if i % REBAL_EVERY == 0:\n            cur_vol = realized_vol(spy_lr, i, VOL_WINDOW)\n            # build median of trailing vols\n            if cur_vol is not None and i >= VOL_WINDOW + MEDIAN_WINDOW:\n                vol_hist = []\n                for j in range(i - MEDIAN_WINDOW + 1, i + 1):\n                    v = realized_vol(spy_lr, j, VOL_WINDOW)\n                    if v is not None:\n                        vol_hist.append(v)\n                med_vol = statistics.median(vol_hist)\n\n                if cur_vol > med_vol:\n                    # HIGH VOL: rotate to stronger safe-haven\n                    gp = trailing_perf(gld_closes, i, PERF_WINDOW)\n                    tp = trailing_perf(tlt_closes, i, PERF_WINDOW)\n                    if gp is not None and tp is not None:\n                        holding = \"GLD\" if gp >= tp else \"TLT\"\n                    else:\n                        holding = \"SPY\"\n                else:\n                    # CALM: hold equities\n                    holding = \"SPY\"\n            else:\n                holding = \"SPY\"\n\n    return rets\n\n# Fetch data (GLD not in listed set; use TLT & AGG as defensive proxies,\n# but strategy asks GLD vs TLT \u2014 attempt GLD, fall back to AGG if unavailable)\nspy = fetch_daily(\"SPY\")\ntlt = fetch_daily(\"TLT\")\ntry:\n    gld = fetch_daily(\"GLD\")\nexcept Exception:\n    gld = fetch_daily(\"AGG\")\n\n# Align lengths to the shortest series\nmin_len = min(len(spy), len(tlt), len(gld))\nspy = spy[-min_len:]\ntlt = tlt[-min_len:]\ngld = gld[-min_len:]\n\n# Benchmark: buy-and-hold SPY over same period\nspy_closes = [b[\"c\"] for b in spy]\nbh = [spy_closes[i] / spy_closes[i-1] - 1 for i in range(1, len(spy_closes))]\nbench = perf_from_daily_returns(bh)\n\nrets = vol_rotation_strategy(spy, gld, tlt)\nm = perf_from_daily_returns(rets)\n\nshow(\"SPY Vol-Regime Rotation (SPY / GLD-TLT)\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmpn_9tfyrd.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Turn-of-Month SPY Effect": {
      "strategy": "Turn-of-Month SPY Effect",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport math\n\n# ---------------------------------------------------------------------------\n# STRATEGY: \"Turn-of-the-Month\" (TOM) seasonal effect\n# ---------------------------------------------------------------------------\n# RATIONALE:\n#   Empirically, equity returns are disproportionately concentrated around the\n#   turn of the month. Institutional cash flows (pension contributions,\n#   payroll 401k inflows, fund reinvestments, window-dressing) cluster at\n#   month-end and the first few days of the new month, creating a persistent\n#   buy-side pressure on broad equity indices like SPY.\n#\n#   ENTRY:  Buy SPY at the CLOSE of the LAST trading day of each month.\n#           WHY: capture the inflow-driven demand that begins at month turn.\n#   HOLD:   Through the THIRD trading day of the new month.\n#           WHY: the seasonal edge historically decays after ~3 trading days.\n#   EXIT:   At close of 3rd trading day -> move to cash/BND proxy (AGG here).\n#           WHY: avoid mid-month drift with no seasonal tailwind; earn a small\n#                bond carry (AGG) while flat on equities.\n# ---------------------------------------------------------------------------\n\n\ndef _month_key(bar):\n    # Alpaca daily bar 't' is an ISO timestamp string like '2016-01-04T05:00:00Z'\n    ds = bar[\"t\"][:10]  # YYYY-MM-DD\n    return ds[:7]       # YYYY-MM  -> month bucket\n\n\ndef _build_hold_flags(bars):\n    \"\"\"\n    Return a boolean list `hold[i]` == True if we should be LONG SPY\n    during the return period from day i-1 -> i (i.e., held over that day).\n\n    Definition:\n      - Enter at CLOSE of last trading day of month M (index = last_idx).\n      - Exit  at CLOSE of the 3rd trading day of month M+1.\n      - So we EARN SPY returns on the days AFTER entry up to and including\n        the exit-day close.\n    \"\"\"\n    n = len(bars)\n    hold = [False] * n\n\n    # Group indices by month\n    months = {}\n    order = []\n    for i, b in enumerate(bars):\n        k = _month_key(b)\n        if k not in months:\n            months[k] = []\n            order.append(k)\n        months[k].append(i)\n\n    for mi in range(len(order) - 1):\n        cur_month = order[mi]\n        nxt_month = order[mi + 1]\n\n        # Entry: last trading day of current month (buy at its close)\n        entry_idx = months[cur_month][-1]\n\n        # Exit: 3rd trading day of next month (sell at its close)\n        nxt_days = months[nxt_month]\n        exit_pos = min(2, len(nxt_days) - 1)  # 0-based -> 3rd day is index 2\n        exit_idx = nxt_days[exit_pos]\n\n        # We are LONG for the returns realized from (entry_idx+1) ... exit_idx\n        # because return[i] = close[i]/close[i-1]-1 is earned by holding into i.\n        for j in range(entry_idx + 1, exit_idx + 1):\n            if 0 <= j < n:\n                hold[j] = True\n\n    return hold\n\n\ndef turn_of_month(spy_bars, bnd_bars):\n    \"\"\"\n    Long SPY during the turn-of-month window, else hold AGG (bond proxy).\n    Returns daily simple returns list.\n    \"\"\"\n    spy_closes = [b[\"c\"] for b in spy_bars]\n    n = len(spy_bars)\n\n    # Align AGG by date (fallback to 0.0 if a date is missing).\n    bnd_by_date = {b[\"t\"][:10]: b[\"c\"] for b in bnd_bars}\n    bnd_dates = [b[\"t\"][:10] for b in bnd_bars]\n    # Precompute AGG daily returns keyed by date index in SPY calendar\n    # We approximate: on any SPY day, if AGG has prev+cur, use AGG return.\n\n    hold = _build_hold_flags(spy_bars)\n\n    rets = []\n    for i in range(n):\n        if i == 0:\n            rets.append(0.0)\n            continue\n\n        if hold[i]:\n            # Long SPY over this day\n            r = spy_closes[i] / spy_closes[i - 1] - 1.0\n        else:\n            # Flat on equity -> earn AGG (bond) carry if available\n            d_cur = spy_bars[i][\"t\"][:10]\n            d_prev = spy_bars[i - 1][\"t\"][:10]\n            if d_cur in bnd_by_date and d_prev in bnd_by_date:\n                r = bnd_by_date[d_cur] / bnd_by_date[d_prev] - 1.0\n            else:\n                r = 0.0\n        rets.append(r)\n\n    return rets\n\n\n# ---------------------------------------------------------------------------\n# RUN\n# ---------------------------------------------------------------------------\nspy = fetch_daily(\"SPY\")\ntry:\n    bnd = fetch_daily(\"AGG\")\nexcept Exception:\n    bnd = fetch_daily(\"TLT\")\n\n# Benchmark: buy-and-hold SPY over the same period\nbh = [spy[i][\"c\"] / spy[i - 1][\"c\"] - 1.0 for i in range(1, len(spy))]\nbench = perf_from_daily_returns(bh)\n\n# Strategy\nstrat_rets = turn_of_month(spy, bnd)\nm = perf_from_daily_returns(strat_rets)\n\nshow(\"Turn-of-Month SPY (cash/AGG off-window)\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmpcf0ih2xe.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Low-Volatility Sector Tilt": {
      "strategy": "Low-Volatility Sector Tilt",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport statistics\nimport math\n\n# Low-Volatility Sector Rotation Strategy\n# -----------------------------------------\n# WHY: Academic \"low-volatility anomaly\" \u2014 low-vol assets tend to deliver\n# superior risk-adjusted returns. Each month we rank the 11 sector SPDRs by\n# trailing 60-day realized volatility and hold the 3 lowest-vol sectors,\n# equally weighted. Rebalancing monthly captures shifting volatility regimes\n# (e.g., rotating into defensives during turbulent markets).\n\nSECTORS = [\"XLK\", \"XLF\", \"XLE\", \"XLV\", \"XLI\", \"XLP\", \"XLU\", \"XLY\", \"XLB\", \"XLRE\", \"XLC\"]\nVOL_WINDOW = 60   # trailing days for realized volatility\nTOP_N = 3         # hold 3 lowest-vol sectors\n\n\ndef build_date_index(data_by_symbol):\n    \"\"\"Build a common sorted list of dates present across all symbols.\"\"\"\n    date_sets = []\n    for sym, bars in data_by_symbol.items():\n        date_sets.append(set(b[\"t\"][:10] for b in bars))\n    common = set.intersection(*date_sets) if date_sets else set()\n    return sorted(common)\n\n\ndef closes_map(bars):\n    return {b[\"t\"][:10]: b[\"c\"] for b in bars}\n\n\ndef realized_vol(closes, end_idx, window):\n    \"\"\"Std-dev of daily log returns over the trailing window ending at end_idx.\"\"\"\n    if end_idx < window:\n        return None\n    rets = []\n    for j in range(end_idx - window + 1, end_idx + 1):\n        if closes[j - 1] <= 0 or closes[j] <= 0:\n            return None\n        rets.append(math.log(closes[j] / closes[j - 1]))\n    if len(rets) < 2:\n        return None\n    return statistics.pstdev(rets)\n\n\ndef sector_rotation(data_by_symbol, dates):\n    # Per-symbol aligned close arrays over the common dates\n    cmaps = {sym: closes_map(bars) for sym, bars in data_by_symbol.items()}\n    aligned = {}\n    for sym in SECTORS:\n        aligned[sym] = [cmaps[sym][d] for d in dates]\n\n    n = len(dates)\n    rets = []\n    holdings = []  # currently held symbols (equal weight)\n\n    prev_month = None\n\n    for i in range(n):\n        # Daily portfolio return from yesterday's holdings (return realized today)\n        r = 0.0\n        if i > 0 and holdings:\n            per = 0.0\n            for sym in holdings:\n                c_now = aligned[sym][i]\n                c_prev = aligned[sym][i - 1]\n                if c_prev > 0:\n                    per += (c_now / c_prev - 1)\n            r = per / len(holdings)\n        rets.append(r)\n\n        # Determine month boundary \u2192 rebalance at start of each new month\n        cur_month = dates[i][:7]\n        if cur_month != prev_month:\n            prev_month = cur_month\n            # Rank sectors by trailing 60-day realized vol\n            vols = []\n            for sym in SECTORS:\n                v = realized_vol(aligned[sym], i, VOL_WINDOW)\n                if v is not None:\n                    vols.append((v, sym))\n            if len(vols) >= TOP_N:\n                vols.sort(key=lambda x: x[0])\n                # WHY: hold lowest-vol sectors \u2014 low-volatility anomaly favors\n                # more stable, defensive names for better risk-adjusted returns.\n                holdings = [sym for _, sym in vols[:TOP_N]]\n\n    return rets\n\n\n# Fetch data for all sectors\ndata_by_symbol = {}\nfor sym in SECTORS:\n    try:\n        bars = fetch_daily(sym)\n        if bars and len(bars) > VOL_WINDOW + 5:\n            data_by_symbol[sym] = bars\n    except Exception as e:\n        print(\"skip\", sym, e)\n\n# Restrict SECTORS to those actually available\nSECTORS = [s for s in SECTORS if s in data_by_symbol]\n\ndates = build_date_index(data_by_symbol)\n\nrets = sector_rotation(data_by_symbol, dates)\nm = perf_from_daily_returns(rets)\n\n# Benchmark: buy-and-hold SPY over the same common date range\nspy = fetch_daily(\"SPY\")\nspy_map = closes_map(spy)\nspy_dates = [d for d in dates if d in spy_map]\nbh = [spy_map[spy_dates[i]] / spy_map[spy_dates[i - 1]] - 1 for i in range(1, len(spy_dates))]\nbench = perf_from_daily_returns(bh)\n\nshow(\"Low-Vol Sector Rotation (60d, 3 lowest)\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmpnk7veuk_.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "TLT Calendar/Volatility Contraction Breakout": {
      "strategy": "TLT Calendar/Volatility Contraction Breakout",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nfrom math import log\n\ndef true_range(high, low, prev_close):\n    return max(high - low, abs(high - prev_close), abs(low - prev_close))\n\ndef atr(bars, i, period):\n    # ATR over the 'period' bars ending at index i\n    if i < period:\n        return None\n    trs = []\n    for k in range(i - period + 1, i + 1):\n        tr = true_range(bars[k][\"h\"], bars[k][\"l\"], bars[k-1][\"c\"])\n        trs.append(tr)\n    return sum(trs) / len(trs)\n\ndef tlt_volatility_squeeze(bars):\n    \"\"\"\n    Strategy:\n    - Compute 10-day ATR each day.\n    - A 'squeeze' occurs when the current 10-day ATR is the lowest over the last 50 days.\n      Low volatility often precedes a strong directional move (volatility expansion).\n    - After a squeeze, we arm a breakout system:\n        * Go LONG if price breaks above the prior 20-day high  (momentum breakout up).\n        * Go SHORT if price breaks below the prior 20-day low  (momentum breakout down).\n    - Exit rules:\n        * LONG exits on break below 20-day low (opposite band) or 1.5*ATR trailing stop.\n        * SHORT exits on break above 20-day high (opposite band) or 1.5*ATR stop.\n    We use daily close-to-close returns for position accounting.\n    \"\"\"\n    closes = [b[\"c\"] for b in bars]\n    rets = []\n\n    atr_period = 10\n    squeeze_lookback = 50\n    breakout_window = 20\n    stop_mult = 1.5\n\n    position = 0        # +1 long, -1 short, 0 flat\n    entry_price = None\n    entry_atr = None\n    armed = False       # squeeze detected, awaiting breakout\n\n    n = len(bars)\n\n    for i in range(n):\n        r = 0.0\n        # Daily return based on yesterday's position held into today's close\n        if position != 0 and i > 0:\n            raw = closes[i] / closes[i-1] - 1\n            r = position * raw\n        rets.append(r)\n\n        # Need enough history to compute indicators\n        min_hist = max(atr_period + squeeze_lookback, breakout_window) + 1\n        if i < min_hist:\n            continue\n\n        cur_atr = atr(bars, i, atr_period)\n        if cur_atr is None:\n            continue\n\n        # Determine squeeze: current ATR lowest of last 'squeeze_lookback' ATR values\n        atr_series = []\n        for j in range(i - squeeze_lookback + 1, i + 1):\n            a = atr(bars, j, atr_period)\n            if a is not None:\n                atr_series.append(a)\n        if len(atr_series) == squeeze_lookback:\n            if cur_atr <= min(atr_series):\n                # Squeeze -> arm breakout system (why: low vol precedes expansion)\n                armed = True\n\n        # Prior 20-day high/low (excluding today)\n        window = bars[i - breakout_window:i]\n        prior_high = max(b[\"h\"] for b in window)\n        prior_low = min(b[\"l\"] for b in window)\n\n        cur_high = bars[i][\"h\"]\n        cur_low = bars[i][\"l\"]\n        price = closes[i]\n\n        # ---------- Position management ----------\n        if position == 0:\n            if armed:\n                # Enter on breakout of prior band\n                if cur_high > prior_high:\n                    # Break above 20d high after squeeze -> LONG breakout\n                    position = 1\n                    entry_price = price\n                    entry_atr = cur_atr\n                    armed = False\n                elif cur_low < prior_low:\n                    # Break below 20d low after squeeze -> SHORT breakout\n                    position = -1\n                    entry_price = price\n                    entry_atr = cur_atr\n                    armed = False\n\n        elif position == 1:\n            # Exit long: opposite band OR 1.5*ATR stop below entry\n            stop_level = entry_price - stop_mult * entry_atr\n            if cur_low < prior_low:\n                position = 0\n                entry_price = None\n                entry_atr = None\n            elif price < stop_level:\n                position = 0\n                entry_price = None\n                entry_atr = None\n\n        elif position == -1:\n            # Exit short: opposite band OR 1.5*ATR stop above entry\n            stop_level = entry_price + stop_mult * entry_atr\n            if cur_high > prior_high:\n                position = 0\n                entry_price = None\n                entry_atr = None\n            elif price > stop_level:\n                position = 0\n                entry_price = None\n                entry_atr = None\n\n    return rets\n\ndata = fetch_daily(\"TLT\")\nbh = [data[i][\"c\"]/data[i-1][\"c\"]-1 for i in range(1, len(data))]\nbench = perf_from_daily_returns(bh)\nrets = tlt_volatility_squeeze(data)\nm = perf_from_daily_returns(rets)\nshow(\"TLT Volatility Squeeze Breakout\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmpfkp3uo1g.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Oil-Energy Lead-Lag": {
      "strategy": "Oil-Energy Lead-Lag",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport math\n\n# STRATEGY: USO 10-day momentum rotation between XLE (energy equity) and BND (bonds/cash)\n#\n# NOTE ON DATA AVAILABILITY:\n# The requirement states only SPY/QQQ/TLT/AGG are reliably available in the Alpaca\n# daily feed for this environment. USO/XLE/BND may not be present. To keep the\n# strategy INTENT identical (an oil-momentum-driven rotation) while remaining runnable,\n# we map:\n#   USO (oil signal proxy)  -> we attempt USO first, fall back to XLE, then SPY\n#   XLE (risk-on asset)      -> we attempt XLE, fall back to QQQ\n#   BND (risk-off asset)     -> we attempt BND, fall back to AGG, then TLT\n# This preserves the \"oil 10-day momentum drives equity-vs-bond rotation\" logic.\n\ndef try_fetch(symbols):\n    \"\"\"Return (symbol, bars) for the first symbol that returns usable data.\"\"\"\n    for s in symbols:\n        try:\n            bars = fetch_daily(s)\n            if bars and len(bars) > 50:\n                return s, bars\n        except Exception:\n            continue\n    return None, None\n\n\ndef align_by_date(*series_list):\n    \"\"\"Align multiple bar lists on common trading dates. Each series is list of bar dicts with 't' timestamp key.\"\"\"\n    # Build date -> close maps\n    maps = []\n    for bars in series_list:\n        m = {}\n        for b in bars:\n            # try common timestamp keys\n            key = b.get(\"t\") or b.get(\"timestamp\") or b.get(\"date\")\n            m[key] = b[\"c\"]\n        maps.append(m)\n    # Common dates\n    common = set(maps[0].keys())\n    for m in maps[1:]:\n        common &= set(m.keys())\n    dates = sorted(common)\n    aligned = []\n    for d in dates:\n        aligned.append(tuple(m[d] for m in maps))\n    return dates, aligned\n\n\ndef uso_rotation_strategy(signal_closes, xle_closes, bnd_closes):\n    \"\"\"\n    Weekly rebalance (every 5 trading days).\n    Compute trailing 10-day return of the oil-signal asset.\n      - If 10-day return > +5%  -> go LONG XLE (energy equity).\n          WHY: strong oil momentum tends to lift energy equities; ride the trend.\n      - If 10-day return < -5%  -> go to CASH/BND (risk-off bonds).\n          WHY: falling oil signals energy weakness/risk-off; preserve capital in bonds.\n      - Otherwise -> HOLD current position (no whipsaw between thresholds).\n\n    Positions: 'XLE', 'BND', or None (start flat -> treated as BND/cash baseline).\n    Returns daily log-equivalent simple returns list aligned to the input series.\n    \"\"\"\n    n = len(signal_closes)\n    rets = []\n    position = \"BND\"  # start defensively in bonds until a signal fires\n    LOOKBACK = 10\n    REBALANCE = 5  # weekly (approx 5 trading days)\n\n    for i in range(n):\n        r = 0.0\n        if i > 0:\n            if position == \"XLE\":\n                r = xle_closes[i] / xle_closes[i - 1] - 1\n            elif position == \"BND\":\n                r = bnd_closes[i] / bnd_closes[i - 1] - 1\n        rets.append(r)\n\n        # Rebalance decision at weekly boundaries, needs 10-day lookback available\n        if i >= LOOKBACK and i % REBALANCE == 0:\n            mom10 = signal_closes[i] / signal_closes[i - LOOKBACK] - 1\n            if mom10 > 0.05:\n                # Oil up strongly -> risk-on energy equity\n                position = \"XLE\"\n            elif mom10 < -0.05:\n                # Oil down strongly -> risk-off bonds/cash\n                position = \"BND\"\n            # else: hold current position (avoid noise trades in neutral zone)\n\n    return rets\n\n\n# --- Fetch data with fallbacks ---\nsig_sym, sig_bars = try_fetch([\"USO\", \"XLE\", \"SPY\"])\nxle_sym, xle_bars = try_fetch([\"XLE\", \"QQQ\", \"SPY\"])\nbnd_sym, bnd_bars = try_fetch([\"BND\", \"AGG\", \"TLT\"])\n\nif not (sig_bars and xle_bars and bnd_bars):\n    raise SystemExit(\"Required data unavailable from Alpaca feed.\")\n\ndates, aligned = align_by_date(sig_bars, xle_bars, bnd_bars)\nif len(aligned) < 60:\n    raise SystemExit(\"Insufficient aligned data.\")\n\nsignal_closes = [row[0] for row in aligned]\nxle_closes = [row[1] for row in aligned]\nbnd_closes = [row[2] for row in aligned]\n\n# Strategy returns\nstrat_rets = uso_rotation_strategy(signal_closes, xle_closes, bnd_closes)\nm = perf_from_daily_returns(strat_rets)\n\n# Benchmark: buy-and-hold the risk-on asset (XLE proxy) over the same period\nbh = [xle_closes[i] / xle_closes[i - 1] - 1 for i in range(1, len(xle_closes))]\nbench = perf_from_daily_returns(bh)\n\nname = \"USO 10d-Momentum Rotation ({} signal, {} risk-on, {} risk-off)\".format(\n    sig_sym, xle_sym, bnd_sym\n)\nshow(name, m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmp6z8nj0b9.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Turn-of-Month Equity Seasonality": {
      "strategy": "Turn-of-Month Equity Seasonality",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport math\n\ndef turn_of_month_strategy(bars):\n    \"\"\"\n    Turn-of-the-Month effect strategy on SPY.\n\n    RATIONALE:\n    - Equity markets historically exhibit a \"turn-of-the-month\" (TOM) anomaly:\n      returns cluster around the last trading day of a month and the first few\n      trading days of the next month. This is driven by month-end institutional\n      flows (pension contributions, 401k inflows, fund rebalancing, window dressing).\n    - ENTRY: Buy SPY at the close of the LAST trading day of the month, because\n      systematic month-end cash inflows tend to push prices up at the turn.\n    - EXIT: Sell at the close of the 3rd trading day of the new month, because the\n      documented outperformance is concentrated in this narrow window and fades\n      afterward. Staying in cash the rest of the month avoids uncompensated risk.\n    \"\"\"\n    closes = [b[\"c\"] for b in bars]\n    dates = [b[\"t\"][:10] for b in bars]  # ISO date string YYYY-MM-DD\n\n    # Determine year-month for each bar\n    def ym(d):\n        return d[:7]  # 'YYYY-MM'\n\n    n = len(bars)\n\n    # Identify the \"last trading day of month\" indices:\n    # bar i is the last trading day of its month if the next bar is a new month.\n    last_day_of_month = [False] * n\n    for i in range(n):\n        if i == n - 1:\n            last_day_of_month[i] = True\n        elif ym(dates[i]) != ym(dates[i + 1]):\n            last_day_of_month[i] = True\n\n    # Identify the ordinal trading day within each month (1-based).\n    trading_day_of_month = [0] * n\n    counter = 0\n    for i in range(n):\n        if i == 0 or ym(dates[i]) != ym(dates[i - 1]):\n            counter = 1\n        else:\n            counter += 1\n        trading_day_of_month[i] = counter\n\n    rets = []\n    holding = False\n    for i in range(n):\n        r = 0.0\n        if holding and i > 0:\n            # log return while holding position (mark-to-market close-to-close)\n            r = math.log(closes[i] / closes[i - 1])\n        rets.append(r)\n\n        # ENTRY: at close of the last trading day of the month, go long.\n        # (The return of the NEXT day will be captured because holding=True.)\n        if not holding and last_day_of_month[i]:\n            holding = True\n        # EXIT: at close of the 3rd trading day of the new month, go to cash.\n        elif holding and trading_day_of_month[i] == 3:\n            holding = False\n\n    return rets\n\n\ndata = fetch_daily(\"SPY\")\n\n# Buy-and-hold benchmark (log returns) on the same symbol/period\nbh = [math.log(data[i][\"c\"] / data[i - 1][\"c\"]) for i in range(1, len(data))]\nbench = perf_from_daily_returns(bh)\n\nrets = turn_of_month_strategy(data)\nm = perf_from_daily_returns(rets)\n\nshow(\"Turn-of-the-Month SPY\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmp3ycm8ieo.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Gold-Bond Defensive Pair Correlation Flip": {
      "strategy": "Gold-Bond Defensive Pair Correlation Flip",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport math\nfrom datetime import datetime\n\ndef rolling_correlation(x, y):\n    \"\"\"Pearson correlation of two equal-length lists.\"\"\"\n    n = len(x)\n    if n < 2:\n        return 0.0\n    mx = sum(x) / n\n    my = sum(y) / n\n    cov = sum((x[i] - mx) * (y[i] - my) for i in range(n))\n    vx = sum((x[i] - mx) ** 2 for i in range(n))\n    vy = sum((y[i] - my) ** 2 for i in range(n))\n    denom = math.sqrt(vx * vy)\n    if denom == 0:\n        return 0.0\n    return cov / denom\n\ndef gld_tlt_rotation(gld_bars, tlt_bars):\n    \"\"\"\n    Strategy:\n      - Track rolling 60-day correlation between GLD and TLT daily returns.\n      - When correlation turns NEGATIVE (assets diverging = diversification benefit\n        and regime shift) AND GLD's 50-day return exceeds TLT's, overweight GLD 100%\n        (momentum + regime confirmation favoring gold as safe-haven/inflation hedge).\n      - Otherwise hold TLT 100% (treasury bonds as default defensive allocation).\n      - Rebalance monthly (only re-evaluate allocation at the start of each new month).\n\n    Note: GLD is unavailable in the Alpaca set provided, so we proxy the \"gold-like\"\n    risk asset with SPY (equities) and the bond leg with TLT. The same relative-momentum\n    + correlation-regime logic applies: overweight the equity risk asset when it is\n    negatively correlated with bonds and outperforming, else stay in TLT.\n    \"\"\"\n    # Align on common length (both series fetched over same period)\n    n = min(len(gld_bars), len(tlt_bars))\n    gld_c = [gld_bars[i][\"c\"] for i in range(n)]\n    tlt_c = [tlt_bars[i][\"c\"] for i in range(n)]\n    dates = [gld_bars[i][\"t\"] for i in range(n)]\n\n    # Precompute daily returns\n    gld_ret = [0.0] + [gld_c[i] / gld_c[i - 1] - 1 for i in range(1, n)]\n    tlt_ret = [0.0] + [tlt_c[i] / tlt_c[i - 1] - 1 for i in range(1, n)]\n\n    CORR_WIN = 60\n    MOM_WIN = 50\n\n    rets = []\n    holding_gld = False  # False => holding TLT, True => holding GLD (proxy: risk asset)\n    current_month = None\n\n    for i in range(n):\n        # Realize today's return based on yesterday's positioning\n        r = 0.0\n        if i > 0:\n            if holding_gld:\n                r = gld_ret[i]\n            else:\n                r = tlt_ret[i]\n        rets.append(r)\n\n        # Determine month key for monthly rebalance cadence\n        try:\n            dt = datetime.fromisoformat(dates[i].replace(\"Z\", \"+00:00\"))\n            month_key = (dt.year, dt.month)\n        except Exception:\n            month_key = i // 21  # fallback: ~21 trading days per month\n\n        # Rebalance only at the start of a new month, once enough history exists\n        if month_key != current_month and i >= max(CORR_WIN, MOM_WIN):\n            current_month = month_key\n\n            # Rolling 60-day correlation of daily returns\n            gwin = gld_ret[i - CORR_WIN + 1:i + 1]\n            twin = tlt_ret[i - CORR_WIN + 1:i + 1]\n            corr = rolling_correlation(gwin, twin)\n\n            # 50-day total returns\n            gld_mom = gld_c[i] / gld_c[i - MOM_WIN] - 1\n            tlt_mom = tlt_c[i] / tlt_c[i - MOM_WIN] - 1\n\n            # Decision:\n            # Overweight GLD when correlation is negative (regime divergence)\n            # AND GLD momentum beats TLT momentum (relative strength confirmation).\n            if corr < 0 and gld_mom > tlt_mom:\n                holding_gld = True   # risk-on rotation into gold/equity proxy\n            else:\n                holding_gld = False  # default defensive: hold TLT bonds\n\n    return rets\n\n# GLD not in provided set -> use SPY as the risk-asset proxy vs TLT bond leg.\ngld_data = fetch_daily(\"SPY\")\ntlt_data = fetch_daily(\"TLT\")\n\nn = min(len(gld_data), len(tlt_data))\n\n# Benchmark: buy-and-hold TLT (the strategy's default/defensive allocation)\nbh = [tlt_data[i][\"c\"] / tlt_data[i - 1][\"c\"] - 1 for i in range(1, n)]\nbench = perf_from_daily_returns(bh)\n\nrets = gld_tlt_rotation(gld_data[:n], tlt_data[:n])\nm = perf_from_daily_returns(rets)\n\nshow(\"GLD/TLT Correlation-Regime Rotation (SPY proxy)\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmp0eo6zp34.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "Volatility Clustering Regime Switch": {
      "strategy": "Volatility Clustering Regime Switch",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport statistics\nimport math\n\n# ---------------------------------------------------------------------------\n# STRATEGY: Volatility Regime Switching\n#\n# Logic:\n#   - Compute 20-day realized volatility of SPY (std dev of daily log returns).\n#   - Compare current realized vol to its trailing 100-day median.\n#   - LOW VOL regime (rv < median): markets are calm/trending -> hold QQQ\n#         WHY: In calm regimes high-beta growth (QQQ) tends to outperform;\n#              low vol historically correlates with risk-on continuation.\n#   - HIGH VOL regime (rv >= median): markets are stressed -> hold BND\n#         WHY: Elevated vol signals risk-off; rotate into bonds (BND) to\n#              preserve capital and avoid equity drawdowns.\n#   - Rebalance weekly at Friday close (only change allocation on Fridays).\n# ---------------------------------------------------------------------------\n\nRV_WINDOW = 20      # realized vol lookback (days)\nMEDIAN_WINDOW = 100 # median lookback for regime threshold (days)\n\n\ndef build_date_index(bars):\n    \"\"\"Return list of (weekday) for each bar. weekday: Mon=0 ... Sun=6.\"\"\"\n    wds = []\n    for b in bars:\n        # Alpaca timestamps look like \"2016-01-04T05:00:00Z\" or date \"2016-01-04\"\n        t = b[\"t\"]\n        datepart = t[:10]\n        y, m, d = int(datepart[0:4]), int(datepart[5:7]), int(datepart[8:10])\n        # Zeller-free: use math for weekday via ordinal\n        # Python-free weekday computation (no datetime import restriction: allowed but keep simple)\n        wd = day_of_week(y, m, d)\n        wds.append(wd)\n    return wds\n\n\ndef day_of_week(y, m, d):\n    \"\"\"Return weekday Mon=0..Sun=6 using Sakamoto's algorithm.\"\"\"\n    t = [0, 3, 2, 5, 0, 3, 5, 1, 4, 6, 2, 4]\n    if m < 3:\n        y -= 1\n    # Sakamoto returns 0=Sunday..6=Saturday\n    w = (y + y // 4 - y // 100 + y // 400 + t[m - 1] + d) % 7\n    # convert to Mon=0..Sun=6\n    return (w + 6) % 7\n\n\ndef realized_vol(log_rets, i, window):\n    \"\"\"Std dev of the last `window` log returns ending at index i (inclusive).\"\"\"\n    if i < window:\n        return None\n    segment = log_rets[i - window + 1: i + 1]\n    if len(segment) < 2:\n        return None\n    return statistics.pstdev(segment)\n\n\ndef vol_regime_strategy(spy, qqq, bnd):\n    \"\"\"\n    Returns daily arithmetic returns of the switching strategy.\n    Aligns all three series by index (assumes fetch_daily returns aligned\n    trading days for these liquid ETFs).\n    \"\"\"\n    n = min(len(spy), len(qqq), len(bnd))\n    spy = spy[:n]\n    qqq = qqq[:n]\n    bnd = bnd[:n]\n\n    spy_close = [b[\"c\"] for b in spy]\n    qqq_close = [b[\"c\"] for b in qqq]\n    bnd_close = [b[\"c\"] for b in bnd]\n\n    weekdays = build_date_index(spy)\n\n    # SPY log returns for realized vol computation\n    spy_logret = [0.0] * n\n    for i in range(1, n):\n        spy_logret[i] = math.log(spy_close[i] / spy_close[i - 1])\n\n    # Running realized vol series (None until enough data)\n    rv_series = [None] * n\n    for i in range(n):\n        rv_series[i] = realized_vol(spy_logret, i, RV_WINDOW)\n\n    rets = []\n    holding = None  # \"QQQ\", \"BND\", or None (cash)\n\n    for i in range(n):\n        # Daily return based on what we held coming into today\n        r = 0.0\n        if i > 0 and holding == \"QQQ\":\n            r = qqq_close[i] / qqq_close[i - 1] - 1\n        elif i > 0 and holding == \"BND\":\n            r = bnd_close[i] / bnd_close[i - 1] - 1\n        rets.append(r)\n\n        # ---- Rebalance decision: only on Fridays (weekday == 4) at close ----\n        if weekdays[i] != 4:\n            continue\n\n        rv = rv_series[i]\n        if rv is None:\n            continue\n\n        # Need at least MEDIAN_WINDOW valid rv observations up to i\n        valid_rv = [x for x in rv_series[max(0, i - MEDIAN_WINDOW + 1): i + 1]\n                    if x is not None]\n        if len(valid_rv) < 20:  # need a meaningful sample\n            continue\n\n        med = statistics.median(valid_rv)\n\n        # Regime decision\n        if rv < med:\n            # LOW VOL -> risk-on high-beta QQQ\n            holding = \"QQQ\"\n        else:\n            # HIGH VOL -> risk-off bonds BND\n            holding = \"BND\"\n\n    return rets\n\n\n# ---------------------------------------------------------------------------\n# Data fetch\n# ---------------------------------------------------------------------------\nspy = fetch_daily(\"SPY\")\nqqq = fetch_daily(\"QQQ\")\n\n# BND may not be available; fall back to AGG then TLT\ntry:\n    bnd = fetch_daily(\"BND\")\n    if not bnd:\n        raise ValueError\nexcept Exception:\n    try:\n        bnd = fetch_daily(\"AGG\")\n    except Exception:\n        bnd = fetch_daily(\"TLT\")\n\n# Benchmark: buy-and-hold QQQ over the same aligned period\nn = min(len(spy), len(qqq), len(bnd))\nqqq_bh = qqq[:n]\nbh = [qqq_bh[i][\"c\"] / qqq_bh[i -",
      "performance": {},
      "diagnostics": "Syntax error:   File \"/tmp/tmp30vpby5m.py\", line 146\n    bh = [qqq_bh[i][\"c\"] / qqq_bh[i -\n                                 ^\nSyntaxError: '[' was never closed\n"
    },
    "Cross-Sectional Sector Low-Volatility": {
      "strategy": "Cross-Sectional Sector Low-Volatility",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nimport math\nfrom datetime import datetime\n\n# The 11 SPDR sector ETFs\nSECTORS = [\"XLK\", \"XLF\", \"XLV\", \"XLE\", \"XLI\", \"XLY\", \"XLP\", \"XLU\", \"XLB\", \"XLRE\", \"XLC\"]\n\n# Benchmark: SPY (broad market buy-and-hold on the sector universe proxy)\nBENCH_SYMBOL = \"SPY\"\n\nVOL_LOOKBACK = 60   # trailing 60-day realized vol\nN_HOLD = 3          # equal-weight the 3 lowest-vol sectors\n\n\ndef realized_vol(closes, end_idx, lookback):\n    \"\"\"Standard deviation of daily log returns over the trailing `lookback` days\n    ending at end_idx (inclusive). Lower vol => safer/more stable sector.\n    Reason: low-volatility assets tend to have superior risk-adjusted returns\n    (the low-vol anomaly), so we tilt toward the calmest sectors each month.\"\"\"\n    if end_idx < lookback:\n        return None\n    rets = []\n    for j in range(end_idx - lookback + 1, end_idx + 1):\n        if j <= 0:\n            continue\n        p0 = closes[j - 1]\n        p1 = closes[j]\n        if p0 <= 0 or p1 <= 0:\n            continue\n        rets.append(math.log(p1 / p0))\n    if len(rets) < 2:\n        return None\n    mean = sum(rets) / len(rets)\n    var = sum((x - mean) ** 2 for x in rets) / (len(rets) - 1)\n    return math.sqrt(var)\n\n\ndef month_key(bar):\n    \"\"\"Extract YYYY-MM from a bar timestamp so we can detect month changes.\"\"\"\n    ts = bar[\"t\"]\n    # Alpaca timestamps look like \"2016-01-04T05:00:00Z\"\n    return ts[:7]\n\n\ndef build_aligned_data(symbol_bars):\n    \"\"\"Align all sector series and the benchmark on a common set of dates.\n    Returns (dates, closes_by_symbol) where closes_by_symbol[sym] is a list\n    indexed the same as dates.\"\"\"\n    # Build date -> close maps\n    maps = {}\n    for sym, bars in symbol_bars.items():\n        m = {}\n        for b in bars:\n            m[b[\"t\"][:10]] = b[\"c\"]\n        maps[sym] = m\n\n    # Common dates = intersection across all symbols\n    common = None\n    for sym in maps:\n        dates = set(maps[sym].keys())\n        common = dates if common is None else (common & dates)\n    dates = sorted(common)\n\n    closes_by_symbol = {}\n    for sym in maps:\n        closes_by_symbol[sym] = [maps[sym][d] for d in dates]\n    return dates, closes_by_symbol\n\n\ndef low_vol_sector_strategy(dates, closes_by_symbol):\n    \"\"\"Each month, rank sectors by trailing 60-day realized vol, hold the 3\n    lowest-vol sectors equal-weighted, rebalance monthly.\n\n    Entry: at the first trading day of a new month, select the 3 sectors with\n           the LOWEST trailing 60d vol (low-vol anomaly -> better risk-adjusted).\n    Hold:  those 3 equal-weighted until the next month's first trading day.\n    Exit:  implicit each month-start when the selection is refreshed.\"\"\"\n    n = len(dates)\n    rets = [0.0] * n\n\n    held = []  # current basket of symbols\n    prev_month = None\n\n    for i in range(n):\n        # Compute today's portfolio return from yesterday's held basket\n        if i > 0 and held:\n            day_ret = 0.0\n            w = 1.0 / len(held)\n            for sym in held:\n                c = closes_by_symbol[sym]\n                if c[i - 1] > 0:\n                    day_ret += w * (c[i] / c[i - 1] - 1)\n            rets[i] = day_ret\n\n        # Check for month change -> rebalance at start of new month\n        mk = dates[i][:7]\n        if mk != prev_month:\n            prev_month = mk\n            # Rank all sectors by trailing vol using data up to i (inclusive)\n            vols = []\n            for sym in SECTORS:\n                if sym not in closes_by_symbol:\n                    continue\n                v = realized_vol(closes_by_symbol[sym], i, VOL_LOOKBACK)\n                if v is not None:\n                    vols.append((v, sym))\n            if len(vols) >= N_HOLD:\n                vols.sort(key=lambda x: x[0])  # lowest vol first\n                held = [sym for _, sym in vols[:N_HOLD]]\n            # else keep previous basket until enough history exists\n\n    return rets\n\n\n# --- Fetch data ---\nsymbol_bars = {}\nfor sym in SECTORS:\n    try:\n        bars = fetch_daily(sym)\n        if bars and len(bars) > VOL_LOOKBACK + 5:\n            symbol_bars[sym] = bars\n    except Exception:\n        pass\n\nbench_bars = fetch_daily(BENCH_SYMBOL)\n\n# Align sector data\ndates, closes_by_symbol = build_aligned_data(symbol_bars)\n\n# Run strategy\nstrat_rets = low_vol_sector_strategy(dates, closes_by_symbol)\nm = perf_from_daily_returns(strat_rets)\n\n# Benchmark buy-and-hold on SPY over overlapping dates\nbench_map = {b[\"t\"][:10]: b[\"c\"] for b in bench_bars}\nbench_dates = [d for d in dates if d in bench_map]\nbench_closes = [bench_map[d] for d in bench_dates]\nbh = [bench_closes[i] / bench_closes[i - 1] - 1 for i in range(1, len(bench_closes))]\nbench = perf_from_daily_returns(bh)\n\nshow(\"Low-Volatility Sector Rotation (3 lowest-vol SPDRs, monthly)\", m, bench)\n```\n\n",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmplq0526vw.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    },
    "52-Week High Proximity Momentum": {
      "strategy": "52-Week High Proximity Momentum",
      "verdict": "ERROR",
      "code_generated": "```python\nfrom backtest_research import fetch_daily, perf_from_daily_returns, show\nfrom datetime import datetime\n\n# Sector SPDR universe. Given the constraint that only SPY/QQQ/TLT/AGG are\n# reliably available in this environment, we use these as our tradable\n# \"sector proxy\" universe. The strategy logic is identical: each ETF is\n# scored by its price as a % of its trailing 52-week high, and we\n# equal-weight the top 3 closest to their high (momentum / trend-persistence\n# rationale: assets near their 52-week high tend to continue outperforming).\n\nUNIVERSE = [\"SPY\", \"QQQ\", \"TLT\", \"AGG\"]\nTOP_N = 3\nLOOKBACK = 252  # ~52 weeks of trading days\n\n\ndef parse_date(bar):\n    # Alpaca bars have a \"t\" timestamp string like \"2016-01-04T05:00:00Z\"\n    t = bar[\"t\"]\n    return datetime.strptime(t[:10], \"%Y-%m-%d\")\n\n\ndef build_aligned(data):\n    \"\"\"Align all symbols on a common set of trading dates.\n    Returns (dates, closes) where closes[sym] is a list parallel to dates.\"\"\"\n    # Map each symbol's date -> close\n    per_sym = {}\n    for sym, bars in data.items():\n        d = {}\n        for b in bars:\n            d[parse_date(b).date()] = b[\"c\"]\n        per_sym[sym] = d\n\n    # Common dates = intersection so every symbol has a price\n    common = None\n    for sym in UNIVERSE:\n        s = set(per_sym[sym].keys())\n        common = s if common is None else (common & s)\n    dates = sorted(common)\n\n    closes = {sym: [per_sym[sym][dt] for dt in dates] for sym in UNIVERSE}\n    return dates, closes\n\n\ndef month_key(dt):\n    return (dt.year, dt.month)\n\n\ndef sector_high_strategy(dates, closes):\n    \"\"\"Monthly rebalance. Each month-end, compute price/52wk-high for each\n    ETF and hold the TOP_N closest to their 52-week high, equal-weighted.\n\n    WHY: Assets trading near their 52-week high exhibit momentum /\n    trend-persistence; buying strength has historically outperformed\n    buying laggards. Rebalance monthly to keep exposure to the strongest\n    trends and rotate out of decayed names.\"\"\"\n    n = len(dates)\n    rets = [0.0] * n\n    holdings = []  # list of symbols currently held\n\n    for i in range(n):\n        # Apply today's return from yesterday's holdings BEFORE rebalancing\n        if i > 0 and holdings:\n            w = 1.0 / len(holdings)\n            day_ret = 0.0\n            for sym in holdings:\n                day_ret += w * (closes[sym][i] / closes[sym][i - 1] - 1)\n            rets[i] = day_ret\n\n        # Rebalance on the last trading day of each month (month change ahead)\n        is_month_end = (i == n - 1) or (month_key(dates[i]) != month_key(dates[i + 1]))\n\n        # Need enough history to compute a 52-week high\n        if is_month_end and i >= LOOKBACK:\n            scores = []\n            for sym in UNIVERSE:\n                window = closes[sym][i - LOOKBACK + 1: i + 1]\n                hi = max(window)\n                pct_of_high = closes[sym][i] / hi  # closer to 1.0 = near high\n                scores.append((pct_of_high, sym))\n            # Sort descending: highest pct_of_high (closest to 52wk high) first\n            scores.sort(reverse=True)\n            holdings = [sym for _, sym in scores[:TOP_N]]\n\n    return rets\n\n\ndata = {sym: fetch_daily(sym) for sym in UNIVERSE}\ndates, closes = build_aligned(data)\n\n# Benchmark: buy-and-hold SPY over the aligned period\nspy = closes[\"SPY\"]\nbh = [spy[i] / spy[i - 1] - 1 for i in range(1, len(spy))]\nbench = perf_from_daily_returns(bh)\n\nrets = sector_high_strategy(dates, closes)\nm = perf_from_daily_returns(rets)\n\nshow(\"Sector 52-Week High Momentum (Top 3, Monthly)\", m, bench)\n```",
      "performance": {},
      "diagnostics": "Runtime error: Traceback (most recent call last):\n  File \"/tmp/tmpql35c7o1.py\", line 1, in <module>\n    from backtest_research import fetch_daily, perf_from_daily_returns, show\nModuleNotFoundError: No module named 'backtest_research'\n"
    }
  },
  "optimized_strategies": {}
}