name: TJR Strategy (SPY/QQQ Day Trade)

on:
  schedule:
    # Every 2 minutes Mon-Fri inside the NY killzone 9:30–11:00 AM ET (13:30–15:00 UTC)
    # Time Theory: only the killzone window is tradeable
    - cron: "30,32,34,36,38,40,42,44,46,48,50,52,54,56,58 13 * * 1-5"
    - cron: "*/2 14 * * 1-5"
    - cron: "0 15 * * 1-5"
  workflow_dispatch:

jobs:
  tjr:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/charming-edison-b7imkj

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install requests python-dotenv tzdata

      - name: Write .env
        run: |
          cat > .env <<EOF
          ALPACA_API_KEY=${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY=${{ secrets.ALPACA_SECRET_KEY }}
          ALPACA_BASE_URL=https://paper-api.alpaca.markets/v2
          EOF

      - name: Run TJR strategy
        run: python tjr_strategy.py

      - name: Commit state
        run: |
          git config user.name  "Claud-Trade Bot"
          git config user.email "bot@claud-trade"
          git add tjr_state.json 2>/dev/null || true
          git diff --cached --quiet || git commit -m "chore: update TJR state [skip ci]"
          git push origin claude/charming-edison-b7imkj
